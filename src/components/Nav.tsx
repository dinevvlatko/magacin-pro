import { BarChart3, Boxes, ClipboardList, LayoutDashboard, PackageCheck, Settings, ShieldCheck, Truck, Users, Warehouse } from 'lucide-react'
export const nav=[['dashboard','Контрола',LayoutDashboard],['orders','Нарачки',ClipboardList],['packing','Пакување',PackageCheck],['warehouse','Магацин',Warehouse],['clients','Клиенти',Users],['movements','Движења',Truck],['reports','Извештаи',BarChart3],['printing','Архива',Boxes],['settings','Поставки',Settings]] as const
const mobileNav=nav.filter(([id])=>['dashboard','orders','packing','warehouse','clients','reports','printing'].includes(id))
export const SideNav=({page,setPage,isAdmin}:{page:string;setPage:(p:string)=>void;isAdmin:boolean})=><aside className="side"><div className="brand"><div className="brandmark">MP</div><div><strong>MAGACIN PRO</strong><small>Warehouse OS</small></div></div><nav>{nav.map(([id,label,Icon])=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id)}><Icon size={19}/><span>{label}</span></button>)}{isAdmin&&<button className={page==='admin'?'active':''} onClick={()=>setPage('admin')}><ShieldCheck size={19}/><span>Администрација</span></button>}</nav></aside>
export const BottomNav=({page,setPage}:{page:string;setPage:(p:string)=>void})=><nav className="bottom-nav">{mobileNav.map(([id,label,Icon])=><button key={id} className={page===id?'active':''} onClick={()=>setPage(id)}><Icon size={21}/><span>{label}</span></button>)}</nav>
